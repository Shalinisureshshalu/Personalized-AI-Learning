version: 1

frontend:
  phases:
    build:
      commands:
        - echo "Building static site - no compilation required"

  artifacts:
    baseDirectory: ./index.html
    files:
      - '**/*'

  cache:
    paths:
      - node_modules/**/*
      - .cache/**